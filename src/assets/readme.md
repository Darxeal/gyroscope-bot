The `cert.pem` and `key.pem` files is just an ad-hoc SSL certificate, because the [DeviceOrientation API](https://developer.mozilla.org/en-US/docs/Web/API/Window/deviceorientation_event) only works via https.